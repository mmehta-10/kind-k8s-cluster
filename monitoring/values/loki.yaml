# Source: https://github.com/grafana/helm-charts/blob/main/charts/loki-distributed/values.yaml
# limits_config:
#   enforce_metric_name: false
#   reject_old_samples: false
#   #reject_old_samples_max_age:
#   ingestion_rate_mb: 10240
#   ingestion_burst_size_mb: 5120
#   per_stream_rate_limit: 2560MB

# See: https://github.com/grafana/helm-charts/issues/1111#issuecomment-1230049202
loki-distributed:
  ingester:
    extraVolumes:
      - name: loki-chunks
        hostPath:
          path: "/var/loki/chunks"
          type: Directory
    extraVolumeMounts:
      - name: loki-chunks
        mountPath: "/var/loki/chunks"
  querier:
    extraVolumes:
      - name: loki-chunks
        hostPath:
          path: "/var/loki/chunks"
          type: Directory
    extraVolumeMounts:
      - name: loki-chunks
        mountPath: "/var/loki/chunks"